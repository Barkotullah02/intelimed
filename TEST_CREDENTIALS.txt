IntelliMeds — Test User Credentials
====================================
For development/QA testing against the running backend (http://localhost:8080)
and web app (http://localhost:5173). All three were verified working on 2026-09-04.

⚠️  DEV/TEST ONLY — do not use these on any production deployment. Rotate before launch.

------------------------------------------------------------
1) PATIENT
   Email:    pat22229@example.com
   Password: password123
   Role:     ROLE_PATIENT
   Web:      sign in at  #/login   → patient app (dashboard, checker, doctors, consultations, AI)
   Mobile:   sign in on the Flutter app login screen

------------------------------------------------------------
2) DOCTOR  (verified / approved)
   Email:    doc22229@example.com
   Password: password123
   Role:     ROLE_HEALTHCARE_PROFESSIONAL
   Web:      sign in at  #/login   → doctor portal (#/doctor/dashboard)
   Notes:    already approved, so appears in the patient "Doctors" directory
             and can be video/audio consulted.

------------------------------------------------------------
3) ADMIN
   Email:    admin@intellimeds.com
   Password: ChangeMe!123
   Role:     ROLE_ADMIN
   Web:      sign in at  #/admin/login   → admin console
             (dashboard, user management, doctor verification, drugs, interactions)
   Notes:    seeded on startup from app.admin.* in application.properties.

------------------------------------------------------------
Extra admin (owner's personal account):
   Email:    barkotullahopu+admin@gmail.com
   Password: IntelliAdmin#2026
   Role:     ROLE_ADMIN

Tips:
- New professionals register at #/register (choose "Professional") and land in a
  PENDING state until an admin approves them under Admin → Doctor Verification.
- The AI assistant needs a valid GEMINI_API_KEY in springRestApi/.env; without it,
  it falls back to a "consult your doctor" message.
